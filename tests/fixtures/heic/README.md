Place real iPhone HEIC dumps here before production originals are accepted.

The worker uses libheif. The application fails closed when a HEIC cannot be decoded and asks the family to export JPEG from Photos.
