export function ProductPhilosophy() {
  return (
    <section className="section-padding border-t border-border-warm">
      <div className="content-width mx-auto text-center">
        <blockquote className="mx-auto max-w-3xl font-serif text-3xl leading-tight text-charcoal md:text-[2.75rem]">
          The technology should disappear. The person should remain.
        </blockquote>
        <p className="prose-width mx-auto mt-8 text-lg text-warm-grey">
          The QR code is simply the bridge. LifeMarked is designed around the
          story it reveals — not the technology behind it.
        </p>
      </div>
    </section>
  );
}
